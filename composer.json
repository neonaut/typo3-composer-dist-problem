{
	"repositories": [
		{
			"type": "composer",
			"url": "https://composer.typo3.org/"
		}
	],
	"name": "neonaut/test-composer-setup",
	"description": "test",
	"config": {
		"vendor-dir": "dist/vendor",
		"platform": {
			"php": "7.1.1"
		}
	},
	"require": {
		"typo3/cms-core": "^8.7.9",
		"typo3-console/composer-auto-setup": "^0.3.6",
		"typo3-console/php-server-command": "^0.2.0"
	},
	"extra": {
		"typo3/cms": {
			"web-dir": "dist/public"
		}
	}
}
